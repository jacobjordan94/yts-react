version: '3.8'

services:
  yts-react:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yts-react-app
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
